# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'PhotoOverlay' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for PhotoOverlay
    pod 'RxSwift', '6.5.0'
    pod 'RxCocoa', '6.5.0'
    pod 'SnapKit'

  target 'PhotoOverlayTests' do
    # Comment the next line if you don't want to use dynamic frameworks
    use_frameworks!

    # Pods for PhotoOverlayTests
      pod 'RxSwift', '6.5.0'

  end

end
